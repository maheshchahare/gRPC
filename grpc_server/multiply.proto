syntax = "proto3";

package chatbot;

service ChatBot {

  rpc Chat(
    ChatRequest
  ) returns (
    stream ChatResponse
  );

}

message ChatRequest {
  string message = 1;
}

message ChatResponse {
  string reply = 1;
}